// Placeholder module so `tsc -p tsconfig.functions.json` has at least one input file.
// `functions` and the rest of `src/proxy` are created in a later task (per the SDD plan);
// until then, this file keeps `pnpm typecheck` passing. Safe to remove once real content lands.
export {};
